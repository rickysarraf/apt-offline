machine:
  python:
    version: 3.4.2
dependencies:
 pre:
   - sudo apt-get update
   - sudo apt-get install python-dev python3-dev libapt-pkg-dev
   - ./install-aptoffline
 override:
   - sed -i 's|debian/sid|0.9.3|' tox.ini