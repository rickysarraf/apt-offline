# Defaults for apt-offline initscript
# sourced by /etc/init.d/apt-offline
# installed at /etc/default/apt-offline by the maintainer scripts

#
# This is a POSIX shell fragment
#

# Additional options that are passed to the Daemon.
DAEMON_OPTS=""
